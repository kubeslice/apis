---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.17.3
  name: workerslicegateways.worker.kubeslice.io
spec:
  group: worker.kubeslice.io
  names:
    kind: WorkerSliceGateway
    listKind: WorkerSliceGatewayList
    plural: workerslicegateways
    singular: workerslicegateway
  scope: Namespaced
  versions:
  - name: v1alpha1
    schema:
      openAPIV3Schema:
        description: WorkerSliceGateway is the Schema for the slicegateways API
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: WorkerSliceGatewaySpec defines the desired state of WorkerSliceGateway
            properties:
              gatewayConnectivityType:
                default: NodePort
                enum:
                - NodePort
                - LoadBalancer
                type: string
              gatewayCredentials:
                properties:
                  secretName:
                    type: string
                type: object
              gatewayHostType:
                enum:
                - Client
                - Server
                type: string
              gatewayNumber:
                type: integer
              gatewayProtocol:
                default: UDP
                enum:
                - TCP
                - UDP
                type: string
              gatewayType:
                default: OpenVPN
                enum:
                - OpenVPN
                - Wireguard
                type: string
              localGatewayConfig:
                properties:
                  clusterName:
                    type: string
                  gatewayName:
                    type: string
                  gatewaySubnet:
                    type: string
                  loadBalancerIps:
                    items:
                      type: string
                    type: array
                  nodeIp:
                    type: string
                  nodeIps:
                    items:
                      type: string
                    type: array
                  nodePort:
                    type: integer
                  nodePorts:
                    items:
                      type: integer
                    type: array
                  vpnIp:
                    type: string
                type: object
              remoteGatewayConfig:
                properties:
                  clusterName:
                    type: string
                  gatewayName:
                    type: string
                  gatewaySubnet:
                    type: string
                  loadBalancerIps:
                    items:
                      type: string
                    type: array
                  nodeIp:
                    type: string
                  nodeIps:
                    items:
                      type: string
                    type: array
                  nodePort:
                    type: integer
                  nodePorts:
                    items:
                      type: integer
                    type: array
                  vpnIp:
                    type: string
                type: object
              sliceName:
                type: string
            type: object
          status:
            description: WorkerSliceGatewayStatus defines the observed state of WorkerSliceGateway
            properties:
              clusterInsertionIndex:
                type: integer
              gatewayMetrics:
                description: |-
                  GatewayMetrics contains aggregated metrics from the worker cluster gateway pods
                  This field is updated by the worker-operator and provides real-time visibility
                  into gateway performance (latency, throughput, packet loss)
                properties:
                  activePods:
                    description: ActivePods is the number of gateway pods with active
                      (UP) tunnels
                    format: int32
                    type: integer
                  avgLatency:
                    description: AvgLatency is the average latency across all active
                      gateway pods (in milliseconds)
                    format: int64
                    type: integer
                  avgRxRate:
                    description: AvgRxRate is the average receive rate across all
                      active gateway pods (in bytes/sec)
                    format: int64
                    type: integer
                  avgTxRate:
                    description: AvgTxRate is the average transmit rate across all
                      active gateway pods (in bytes/sec)
                    format: int64
                    type: integer
                  lastUpdated:
                    description: LastUpdated is the timestamp when these metrics were
                      last updated
                    format: date-time
                    type: string
                  maxLatency:
                    description: MaxLatency is the maximum latency observed across
                      all active gateway pods (in milliseconds)
                    format: int64
                    type: integer
                  minLatency:
                    description: MinLatency is the minimum latency observed across
                      all active gateway pods (in milliseconds)
                    format: int64
                    type: integer
                  packetLoss:
                    description: PacketLoss is the average packet loss percentage
                      across all active gateway pods
                    format: int64
                    type: integer
                type: object
              gatewayNumber:
                type: integer
            type: object
        type: object
    served: true
    storage: true
    subresources:
      status: {}
